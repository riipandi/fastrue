{
    "name": "driver-postgres",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/driver-postgres",
    "projectType": "library",
    "targets": {
        "dev": {
            "executor": "nx:run-commands",
            "options": {
                "cwd": "packages/driver-postgres",
                "commands": ["node index.js"],
                "parallel": false
            }
        }
    },
    "tags": []
}
